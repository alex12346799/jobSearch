spring:
  application:
  name:
   jobSearch
  datasource:
    url: jdbc:h2:./db/test;AUTO_SERVER=TRUE
    username: root
    password: qwerty
    driver-class-name: org.h2.Driver
    sql:
      init:
        mode: always
server:
  port:
   8089
